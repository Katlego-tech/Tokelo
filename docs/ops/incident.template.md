# INC-nnnn — <what users noticed, in one line>

- Status: open
- Opened: <YYYY-MM-DD HH:MM UTC> · Release: <the live version> · Rolled back to: <version, if any>
- Detected by: <an alert, a user report, the watch window>

<!-- Copy to docs/ops/incidents/INC-nnnn.md with the next number. To close it: set Status to
     closed, add "· Closed: YYYY-MM-DD HH:MM UTC" to the Opened line, write the postmortem, and
     name the change below. scripts/realm/realm incidents checks all three. -->

## What happened

## Timeline

- <time>: <event>

## Postmortem

<!-- Blameless: what in the system let this happen, and why nothing caught it sooner. -->

## The change that stops it recurring

<!-- The commit (its hash) or the PR (its URL) that changed a file so this can't happen again.
     A resolution to "be careful" isn't one. -->
